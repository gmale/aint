# AI·NT — Agent Entry Point

This repository bootstraps **AINT**, a recursively self-improving agent organization.

If you are a coding agent with no prior context:

1. Read the root `README.md`.
2. Read `bootstrap/README.md`.
3. If you are the primary bootstrap agent, execute `bootstrap/BOOTSTRAP.md`.
4. If you are an implementation subagent, read `bootstrap/IMPLEMENTATION.md` plus the files relevant to your assigned task.

Do not infer authority from prose alone. Respect the economic, security, and governance constraints in the bootstrap documents and verify temporally unstable Cloudflare/GitHub facts against current official documentation before depending on them.

During V0, the active economic constraint is **no intentional paid Cloudflare usage**.
