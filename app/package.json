{
  "name": "aint-app",
  "private": true,
  "description": "AI·NT application surface: static UI + Worker API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler types --check && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.124.0"
  }
}
